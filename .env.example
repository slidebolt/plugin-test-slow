# plugin-test-slow environment
# Test plugin with slow discovery for testing timeouts.
# For local discovery mode, only PLUGIN_DATA_DIR is required.

# Required: Directory for plugin state storage
PLUGIN_DATA_DIR=/tmp/plugin-test-slow
